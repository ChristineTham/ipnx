# chunk 014

COMPLETE. All 220 files read in full (verified `listed=220 present=220`).
220 = 116 `v10/usr/include/sys/*.h` + 15 `v10/usr/include/sys/inet/*.h` + 89 `v10/usr/jerq/**`
(5 dotfiles, `demo/mux/demo`, `demo/src/` = `.comfile` + makefile + 32 `.c`/`.s`,
`asteroids/` 14, `cent/` 22, `fence/` 10).

## install destinations
| file:line | destination | what | quoted evidence |
| --- | --- | --- | --- |
| (whole subtree) `v10/usr/include/sys/*` | `/usr/include/sys` | the 131 public copies of the kernel headers | tree position is the statement; `v10/` mirrors the guest filesystem |
| `v10/usr/include/sys/lnode.h:11` | `/usr/src/.../PATCHES.md` (see traps) | pointer to the reconstruction record | `" * v10/src/PATCHES.md for the disassembly."` |
| `v10/usr/src/build/src/include/sys/lnode.h` -> `v10/usr/include/sys/lnode.h` | `/usr/include/sys/lnode.h` | ipnx-reconstructed Share-scheduler header; the build tree carries the master copy and it must be installed into `/usr/include/sys` | the two files are byte-identical (blob `b465c8e8`), separate inodes |
| `v10/usr/include/sys/inet/tcp_user.h:2` | `/dev/tcp*` | the TCP device nodes the header describes the protocol for | `" * things users need to know to talk to /dev/tcp*"` |
| `v10/usr/include/sys/inet/tcp_user.h:9-10` | `/usr/include/sys/inet/` | in.h and tcp.h must be reachable as `<sys/inet/...>`, i.e. the `inet` subdirectory must be installed, not flattened | `#include <sys/inet/in.h>` / `#include <sys/inet/tcp.h>` |
| `v10/usr/include/sys/inet/udp_user.h:2` | `/dev/udp*` | the UDP device nodes | `" * things users need to know to talk to /dev/udp*"` |
| `v10/usr/include/sys/inet/udp_user.h:9-10` | `/usr/include/sys/inet/` | same requirement for udp | `#include <sys/inet/in.h>` / `#include <sys/inet/udp.h>` |
| `v10/usr/include/sys/user.h:1-3,131` | `/usr/include/sys/`, `/usr/include/errno.h` | user.h pulls four other installed headers by angle-bracket path | `#include <sys/pcb.h>`, `#include <sys/dmap.h>`, `#include <sys/vtimes.h>`, `#include <errno.h>` |
| `v10/usr/include/sys/vm.h:5-8` | `/usr/include/sys/` | vm.h is a convenience umbrella over four installed headers | `" * #include <sys/vm.h>"` / `#include <sys/vmparam.h>` … |
| `v10/usr/jerq/.profile:6` | `/usr/jerq/bin`, `/usr/andrew/bin`, `/usr/rob/bin`, `/bin`, `/usr/bin` | the jerq user's search path; `/usr/jerq/bin` is where the host-side jerq commands are expected | `PATH=:/usr/jerq/bin:/usr/andrew/bin:/usr/rob/bin:/bin:/usr/bin` |
| `v10/usr/jerq/.profile:5` | `/etc/whoami` | the hostname file, read to build the prompt | `PS1=` + backtick `cat /etc/whoami` + `'$ '` |
| `v10/usr/jerq/.profile:10` | `/usr/rob/qed/q/qfile` | qed's save file (`QEDFILE`) | `QEDFILE=/usr/rob/qed/q/qfile` |
| `v10/usr/jerq/.profile:8` | `/tmp/jerq.$$` | per-shell history file | `HISTORY=/tmp/jerq.$$` |
| `v10/usr/jerq/.blit:1-2` | `/usr/blit/bin` | the *Blit* commands, with `TERM=blit` | `TERM=blit` / `PATH=:/usr/blit/bin$PATH` |
| `v10/usr/jerq/demo/mux/demo:1-2` | `/bin`, `/usr/bin`, `/usr/jerq/bin`; `JPATH=/usr/jerq/demo/mux` | launcher's own path, and the directory `32ld`/`jx` download demos from | `PATH=/bin:/usr/bin:/usr/jerq/bin` / `JPATH=/usr/jerq/demo/mux` |
| `v10/usr/jerq/demo/mux/demo:6` | `/usr/jerq/demo/mux` | the directory listing IS the demo menu | `ls /usr/jerq/demo/mux\| egrep -v "demo\|jciferr" \|pr -t -6 -l1` |
| `v10/usr/jerq/demo/mux/demo:14` | `/usr/jerq/bin/jcif` (program) + `./jcif` (data, in `/usr/jerq/demo/mux`) | jcif viewer and its data file | `/usr/jerq/bin/jcif ./jcif` |
| `v10/usr/jerq/demo/mux/demo:26` | `/usr/jerq/demo/mux/$1` | flick files played by `fflicks` (horse, dodec, explode, arno) | `horse\|dodec\|explode\|arno)	fflicks /usr/jerq/demo/mux/$1` |
| `v10/usr/jerq/demo/src/makefile:1-3` | `/usr/dud/bin` | declared home of the cross compiler/assembler `3cc`/`3as` | `BIN=/usr/dud/bin` / `CC=$(BIN)/3cc` / `AS=$(BIN)/3as` |
| `v10/usr/jerq/demo/src/makefile:8,11,14,23,26,38,41` | `/usr/jerq/lib/l.o` | the terminal-side crt0 every `mld` link starts with | `mld -o fake -b 256 /usr/jerq/lib/l.o fake.o -lsys -lj -lc` |
| `v10/usr/jerq/demo/src/asteroids/makefile:14-17` | `/usr/jerq/demo/mpx/asteroids` | the stripped, optimised terminal binary | `install:	a` / `mopt a` / `mstrip m.out` / `cp m.out /usr/jerq/demo/mpx/asteroids` |
| `v10/usr/jerq/demo/src/asteroids/makefile:3` | `/usr/jerq/bin` | declared (but unused) tool directory | `BIN=/usr/jerq/bin` |
| `v10/usr/jerq/demo/src/asteroids/makefile:10` | `/usr/jerq/include` | the jerq headers, needed by lint and by the compiler | `lint -p -I/usr/jerq/include $(FILES)` |
| `v10/usr/jerq/demo/src/cent/Makefile:7` | `/usr/jerq/bin/3cc` | the compiler, by absolute path | `CC=/usr/jerq/bin/3cc` |
| `v10/usr/jerq/demo/src/cent/Makefile:26` | `/usr/jerq/include` | jerq headers for lint | `lint -I/usr/jerq/include $(CFILES)` |
| `v10/usr/jerq/demo/src/cent/Makefile:23-24` | `cent.m` | the centipede terminal binary; a `.m` belongs beside `v10/usr/jerq/mbin/*.m` | `cent.m: playdefs.h $(OFILES)` / `	$(CC) $(LDFLAGS) -o cent.m $(OFILES)` |
| `v10/usr/jerq/demo/src/cent/Makefile:47` | `cent.cpio` | the source-distribution archive, and the manifest it obeys | `ls cent.README Makefile playdefs.h *.c \| cpio -oc > cent.cpio` |
| `v10/usr/jerq/demo/src/fence/makefile:3-4` | `fence.m` | the fence terminal binary | `fence.m:	fence.o` / `	3cc fence.o -o fence.m` |
| `v10/usr/jerq/demo/src/fence/fence.c:22-46` | `fence1l` … `fence4r` (source directory) | eight icon files pulled in as C initialiser text at COMPILE time, so they must sit beside `fence.c`, not on the running system | `#include "fence1l"` … `#include "fence4r"` |
| `v10/usr/jerq/demo/src/cent/playdefs.h:1-2` | `/usr/jerq/include/jerq.h`, `/usr/jerq/include/font.h` | every `cent` object needs both, by quoted name | `#include "jerq.h"` / `#include "font.h"` |
| `v10/usr/jerq/demo/src/mazewar.c:5` | `/tmp/maze` | the umpire rendezvous file both players open r/w | `char *umpire = "/tmp/maze";` |
| `v10/usr/jerq/demo/src/mazewar.c:6` | `/usr/jerq/demo/maze` | the terminal-side maze binary that gets downloaded | `char *game = "/usr/jerq/demo/maze";` |
| `v10/usr/jerq/demo/src/mazewar.c:38` | `/usr/jerq/lib/term` | the terminal program restored on exit | `boot("/usr/jerq/lib/term");` |
| `v10/usr/jerq/demo/src/mazewar.c:46` | `/usr/jerq/bin/68ld` | the 68000 downloader, invoked by absolute path | `if(system("/usr/jerq/bin/68ld", "68ld", "-", s))` |
| `v10/usr/jerq/demo/src/eyes.c:10,13,14` | (documented invocation) | build and run recipe using the 630 toolchain | `$ dmdcc -o eyes.m eyes.c	# -g -O also recommended` / `$ dmdld eyes.m` / `$ dmdld eyes.m -l` |
| `v10/usr/jerq/demo/src/road.c:21,36-41,141-142` | `/usr/jerq/font/EB.24`, `EB.18`, `EB.12`, `EB.10`, `EB.8` | a table naming five font data files and the directory they must live in | `char *fontdir = "/usr/jerq/font";` and `sprintf(buf, "%s/%s", fontdir, fnts[i].name); fp = getfont(buf);` -- all five exist in `v10/usr/jerq/font/` |
| `v10/usr/jerq/demo/src/rose.c:22-30` | `/usr/jerq`, `$(JERQ)/include`, `$(JEQ)/bin`, `$(JERQBIN)/mcc` | an embedded makefile giving the jerq root, include dir, bin dir and compiler | `" * JERQ=/usr/jerq"` / `" * JERQINCLUDE=$(JERQ)/include"` / `" * JERQBIN=$(JEQ)/bin"` / `" * MCC=$(JERQBIN)/mcc"` |
| `v10/usr/jerq/demo/src/rose.c:20,27-30` | `rose.m` and `pararose.m` | two products from one source, run with the 68000 loader | `" *              68ld rose.m     or      68ld pararose.m"` / `$(MCC) -DNONSTOP=1 rose.c -o rose.m` / `$(MCC) -DNONSTOP=0 rose.c -o pararose.m` |
| `v10/usr/jerq/demo/src/clock.c:32` | (documented invocation) | how the clock demo must be started, matching `demo/mux/demo:16` | `string(&defont,"Usage: 32ld clock \"`date`\"", ...)` |

## documented build steps

- `v10/usr/include/sys/param.h:131` includes types.h with a QUOTED, directory-qualified
  path: `#include "sys/types.h"`. From `/usr/include/sys/param.h` that first tries
  `/usr/include/sys/sys/types.h` (absent) and only then the `-I` path; from the kernel's
  `/usr/sys/sys/param.h` with `-I/usr/sys` it resolves to `/usr/sys/sys/types.h`. The one
  line therefore works in both trees only because of the directory naming -- it is not
  interchangeable with `<sys/types.h>`.
- `v10/usr/include/sys/param.h:37-39` conditionally pulls `<signal.h>`:
  `#ifndef NSIG` / `#include <signal.h>` -- so `/usr/include/signal.h` is a prerequisite of
  compiling anything that includes `<sys/param.h>`.
- **The jerq demos are cross-compiled for the terminal, not the VAX.** Four different
  toolchains are named across four makefiles in one tree: `mcc`/`mld` (from `PATH`),
  `3cc`/`3as` (`/usr/dud/bin/3cc`, plain `3cc`, and `/usr/jerq/bin/3cc`), plus `mopt`,
  `mstrip`, `lint`, `pp`, `dcan`, `8dcan`, `opr`, `cpio`. Products are named `*.m`
  (`cent.m`, `fence.m`) or `m.out`, matching `v10/usr/jerq/mbin/*.m`; all three
  (`mbin/sam.m`, `demo/src/cent/cent.m`, `demo/src/asteroids/a`) start with the same
  4-byte magic `70 01 03 00`, so `.m` is the terminal's own a.out variant.
- `demo/src/makefile:8` documents the link recipe: `mld -o NAME -b 256
  /usr/jerq/lib/l.o NAME.o -lsys -lj -lc` -- load base `-b 256`, crt0 `l.o` first,
  library order `-lsys -lj -lc`, with `-llayer` inserted before `-lj` for the windowed
  demos (`:26`, `:29`).
- `demo/src/makefile:31-35` is an INSISTED-ON post-compile edit of generated assembler:
  `mcc -S balloc.c` then `sed '/divs.w/s/.*/	asr.l	\&1, %d0/' < balloc.s > goo` /
  `mv goo balloc.s` / `mcc -c balloc.s`. The compiler emits a `divs.w` the target cannot
  use and the build rewrites it to an arithmetic shift. `.s` is generated here, so a
  committed `balloc.s` would be shadowed -- there is none.
- `demo/src/cent/Makefile:21` states the whole build: `# 'make' is all you have to make
  for this to work.` Default target is `cent.m: playdefs.h $(OFILES)`.
- `demo/src/asteroids/makefile:12` `pp -kjerq -keywords -t"Asteroids" event.h rock.h
  ship.h $(FILES) | dcan` -- the `-keywords` argument is satisfied by the file
  `asteroids/eywords`, 26 bytes, four lines: `Rock`, `Event`, `Explosion`, `Bomb`. The
  name is not a typo to be fixed: `pp` takes `-k<file>`, so `-keywords` means "keywords
  file = `eywords`". A rename would break the pretty-printer target.
- **Per-file compile lines in a leading `%` comment.** `fireworks.c:1`
  `/*% cyntax -Dlint -j % && 3cc -go # %` and `lunch.c:1` `/*% 3cc -go # %` -- an editor/
  build-helper convention where `%` is the source and `#` the output. It names a fifth
  tool, `cyntax`. `demo/src/.comfile` is the list of files carrying such a line: two
  entries, `lunch.c` and `runch.c`.
- **`eyes.c:9-14` documents its own build with a DIFFERENT toolchain from the makefile in
  the same directory**: `dmdcc`/`dmdld`, which are `v10/usr/630/bin/dmdcc` and
  `v10/usr/630/bin/dmdld` with man pages under `v10/usr/630/man/`. Its `#ifdef DMD630`
  branch (`:38-41`) includes `<dmd.h>` and `<5620.h>`, which exist only as
  `v10/usr/630/include/dmd.h` and `v10/usr/630/include/5620.h`. So building `eyes.c` needs
  `-I/usr/630/include`, not the `-I/usr/jerq/include` every makefile here specifies, and
  `eyes` is not a target in `demo/src/makefile` at all.
- **`rose.c:17-31` carries an entire makefile inside its header comment**, the only build
  instructions for that file, and states the run command
  `68ld rose.m     or      68ld pararose.m`. Two products come from one source via
  `-DNONSTOP=1` / `-DNONSTOP=0`.
- **No makefile in this tree passes `-I` to the compiler** -- `asteroids/makefile:10` and
  `cent/Makefile:26` pass `-I/usr/jerq/include` to `lint` only. Yet `win.c:2`
  `#include "layer.h"` and `batcomp.c:2` `#include "jerqio.h"` are quoted includes of
  headers that exist only as `v10/usr/jerq/include/layer.h` and
  `v10/usr/jerq/include/jerqio.h`, never in the source directory. So `<jerq.h>` and these
  resolve entirely through `mcc`/`3cc`'s compiled-in default include directory.
- **Compile-time switches no makefile here sets.** `MUX` gates cent's whole
  layer-size check and RESHAPED handling (`init_scr.c:13-44`, `player.c:23-31`,
  `personchoose.c:25-30`); `mc68000` selects the direct-poke `beep`
  (`playdefs.h:17-21`: `#define beep (*((char*)(384*1024L+062)) = 2) /* blit only */`
  vs `ringbell()`); `DMD630` selects the 630 headers in `eyes.c:38`; `MPX` selects a
  filled disc over a circle in `rae.c:31-36`; `NONSTOP` picks `rose.m` vs `pararose.m`;
  `lint` turns `Pt`/`Rect`/`Rpt` back into functions (`jerq.h:72-77`). `cent/Makefile:6`
  and `asteroids/makefile:5` both set `CFLAGS=` empty, so none of these is defined by the
  committed build.
- `KERNEL`, `IN_PARANOID`, `TCPOUTFLAGS`, `TCPSTATES`, `TCPTIMERS`, `KPROF` and `CLSIZE`
  are the compile-time switches these headers key off. Note `tcp_fsm.h:35` and
  `tcp_timer.h:71` DEFINE ARRAYS (`u_char tcp_outflags[]`, `char *tcptimers[]`) rather than
  declaring them, so exactly one .c file may define `TCPOUTFLAGS`/`TCPTIMERS`.
- `v10/usr/include/sys/vmmeter.h:82-97`, `systm.h:6-20`, `proc.h:79-80`, `text.h:27`,
  `buf.h:63-70` and others declare *tentative definitions* (no `extern`) inside
  `#ifdef KERNEL`, which is how this kernel allocates its globals -- the pre-ANSI
  common-block model. Compiling two of these into one object with a modern
  `-fno-common` compiler would fail; V10's own cc will not.

## contradictions and traps

- **The tools every makefile here calls are tracked ONLY under `/usr/blit/bin`, are
  MISSING FROM THE WORKING TREE, and `3cc` does not exist at all.** `git ls-files v10`
  finds `mcc`, `mld`, `mopt`, `mstrip` at exactly `v10/usr/blit/bin/mcc`, `.../mld`,
  `.../mopt`, `.../mstrip` and nowhere else -- but none of the four is on disk: `git
  status` reports them ` D`, along with 28 other files under `v10/usr/blit/bin`, 46 under
  `v10/usr/630/bin` and **957 in all under `v10/`** (`git status --porcelain v10/ | grep
  -c '^ D'`). The same is true of `jcif`, `jx`, `68ld`, `dmdcc` and `dmdld`. Meanwhile
  `3cc`/`3as` (named by `asteroids/makefile:4`, `cent/Makefile:7`, `fence/makefile:1,4`,
  `demo/src/makefile:2-3`) and `dcan`/`8dcan` (`asteroids/makefile:12`,
  `cent/Makefile:31`) are not tracked anywhere in the repository, and `cyntax`
  (`fireworks.c:1`) exists only as source under `v10/usr/src/cmd/cyntax/`.
  So the jerq demo build depends on the *blit* tree's binaries; nothing in
  `v10/usr/jerq/` puts `/usr/blit/bin` on PATH except `.blit` -- the file with the missing
  path separator -- and right now nothing on disk supplies any of them.
  (This worktree-deletion caveat does NOT affect the absence findings below: the
  `/usr/jerq/bin`, `/usr/jerq/lib`, `/usr/jerq/demo/mpx`, `/usr/jerq/demo/maze`,
  `/usr/dud`, `cent.README`, `rose.m` and `pararose.m` paths have **zero tracked files**,
  so they are absent from the distribution, not merely from the checkout. The two
  duplicate-tree comparisons were re-run against git blobs rather than the worktree and
  gave identical counts: 130/128/2 and 50/25/25.)
- **`asteroids/` overrides three `<jerq.h>` macros with real functions, and ONE FILE
  MISSES THE OVERRIDE.** `v10/usr/jerq/include/jerq.h:76` makes `Pt` a macro and
  `:139-140` makes `transform`/`rtransform` IDENTITY macros:
  `#define transform(p)	(p)` / `#define rtransform(r)	(r)`.
  `asteroids/rock.h:11-16` cancels all three -- `#undef Pt` / `extern Point Pt();` /
  `#undef transform` / `extern Point transform();` / `#undef rtransform` /
  `extern Rectangle rtransform();` -- and `bomb.c:75-104` supplies the real
  `Pt`, `transform` (which scales by `P->rect`) and `rtransform`. This only works in a
  file that includes `rock.h` AFTER `<jerq.h>`. Five of the eight sources do
  (`a.c:1,4`, `batcomp.c:1,3`, `bomb.c:1,3`, `rock.c:1,2`, `ship.c:1,3`), but
  **`explode.c` includes only `<jerq.h>` and `"event.h"` (`:1-2`) and then calls
  `transform(ex[i].p)` at `:220`** -- so every explosion is blitted through the identity
  macro, unscaled, while the ships, rocks and bombs around it are scaled. Nothing
  diagnoses it; the program just draws explosions in the wrong place in a resized layer.
  `image.c` adds a THIRD scheme, its own `pscale`/`rscale` (`:18-37`) keyed off `Drect`
  rather than `P->rect`.
- **The jerq copy of `asteroids/bomb.c` is the one that carries those functions.**
  Against `v10/usr/blit/demo/src/asteroids/bomb.c` (1982 vs jerq's 1984) the only
  differences are `nmoves<=0` -> `nmoves==0` and the 37 added lines defining `Pt`,
  `transform`, `rtransform` and `jrectf`. So the two trees' `asteroids` are not
  interchangeable file by file.
- **FIVE incompatible definitions of `Pt(x, y)` coexist, two of them byte-swapped.**
  `v10/usr/jerq/include/jerq.h:76`, `include/old/jerq.h:69` and `include/ojerq.h:69` pack
  **x in the high half**; `v10/usr/src/libj/jerq.h:35` packs **y in the high half**
  (`((((short)(y))<<16)|((unsigned short)(x)))`); `v10/usr/blit/include/jerq.h:70` is not
  a packed word at all (`(short)(x), (short)(y)`); `include/CC/jerq.h.BE:137` is
  `Point(x,y)`. `Point` is `struct { short x; short y; }` (`jerq.h:34-37`), so the two
  packed forms are the big-endian (68000, terminal side) and little-endian (VAX, host
  side) spellings of the same struct -- `src/libj` is the host `-lj` source. Which header
  `-I` finds therefore decides whether coordinates arrive swapped, and nothing diagnoses
  the wrong choice.
- **`rose.c`'s embedded makefile has an undefined variable.** `:24` is
  `JERQBIN=$(JEQ)/bin` -- `JEQ`, not `JERQ`, which is defined two lines above. `$(JEQ)`
  expands to nothing, so `JERQBIN=/bin` and `MCC=/bin/mcc`. Neither `rose.m` nor
  `pararose.m` is tracked anywhere in the repository.
- **`rose.c:39-49` stubs out the very library routines its interactive mode needs.**
  `jmoveto(p) Point p; { }` and `Point jstring(s) char *s; { Point p; return(p); }` -- an
  empty move and a string routine returning an uninitialised `Point`. So the
  `-DNONSTOP=0` product (`pararose.m`), whose whole purpose is to prompt for `loops=` and
  `delta=` through `jstring`/`getnum` (`:70-92`, `:131-184`), can never echo anything.
- **`v10/usr/jerq/demo/src` holds near-duplicate pairs that differ by one line.**
  `munch.c` and `one.c` are identical but for `#define DELTA 13` vs `DELTA 1` (verified
  with `diff`); `ball.c` and `nball.c` differ only in four raster-op constants
  (`F_XOR`/`F_STORE` vs `F_CLR`/`F_OR`). `Munch.s` is the hand-saved 68000 output of
  `munch.c` (`add.l &13,%d5` at `Munch.s:51` is `DELTA 13`) sitting beside its own source.
- **`Munch.s` and `munch.c` share one object name on a case-insensitive filesystem.**
  Both are in `v10/usr/jerq/demo/src/`, and `Munch.o` / `munch.o` fold together -- the only
  such collision in the tree (checked per directory over every `.c`/`.s`). This repository
  is on APFS.
- **Three ROM addresses for two routines, in one directory.** `magnet.c:6`, `pogo.c:9` and
  `tracks.c:8` all `#undef bitblt` and redefine it as
  `(*((void(*)())0x7a24))(...)`, agreeing on `bitblt`. But the commented-out `texture`
  override is `0x4372c` in `magnet.c:7` and `0x8d84` in `pogo.c:10` -- two different
  firmware revisions recorded side by side.
- **`norm.s:59` keeps the instruction the makefile seds out of compiled code.** The
  hand-written `sqrtryz` uses `divs.w %d3,%d0`, while `makefile:33` rewrites exactly
  `divs.w` to `asr.l &1, %d0` in `balloc.s`. `norm.o` is linked into `spheres`
  (`makefile:22-23`) without that treatment.
- **`road.c`'s font table is dead code.** `loadfont()` at `:128-147` begins
  `return(&defont);` at `:134` and the entire directory-reading body -- the `sprintf`,
  the `getfont`, the size selection -- is commented out at `:135-146`. The
  `/usr/jerq/font` path and the five `EB.*` names survive only as data.
- **`mazewar.c` is the one HOST program in a directory of terminal programs, and every
  path it hardcodes is missing.** `:1-3` includes `"stdio.h"`, `"sgtty.h"`, `"signal.h"`
  (VAX headers, quoted), so it compiles with the host cc -- yet it has no target in
  `demo/src/makefile`. Of its four hardcoded paths, `/usr/jerq/demo/maze` (`:6`),
  `/usr/jerq/lib/term` (`:38`) and `/usr/jerq/bin/68ld` (`:46`) are all absent:
  `git ls-files 'v10/usr/jerq/demo/maze*'` = 0, `git ls-files 'v10/usr/jerq/lib*'` = 0,
  and the only `68ld` in the tree is `v10/usr/blit/bin/68ld` (source
  `v10/usr/blit/src/68ld/68ld.c`). `v8/jerq/lib/` DOES exist in this repository (89 files)
  while `v10/usr/jerq/lib/` does not -- the directory these V10 sources depend on survives
  only in the Eighth Edition tree, and even there it holds no `l.o` and no `term`.
- **Two incompatible downloaders are named from one directory.** `demo/mux/demo` uses
  `32ld` (WE32100, `v10/usr/jerq/src/32ld/32ld.c`) and `clock.c:32` repeats it, while
  `mazewar.c:46` uses `68ld` (68000). The demo sources themselves are 68000 --
  `Munch.s` is Motorola syntax (`mov.l &2000, %a2`, `lsl.l`, `rts`) and
  `demo/src/makefile:33` seds a `divs.w` into `asr.l &1, %d0`.
- **`magnet.c:6` replaces a library call with a hardcoded firmware address.**
  `#undef bitblt` then `#define bitblt(s, r, d, p, c) (*((void(*)())0x7a24))(s,r,d,p,c)`,
  with a commented-out `texture` at `0x4372c` (`:5,7`). The demo is bound to one ROM
  revision and will not survive relinking against `-lj`.
- **Absolute terminal addresses are baked into several demos.** `balloc.c:6-7`
  `STARTAREA (Word *)(90*1024)` / `ENDAREA (Word *)(156*1024-1024)` (`" -1024 is for
  stack (not much!)"`); `munch.c:5-9` `DADDR ((short *)(384L*1024L+030))`,
  `DSTAT ...+040`, `START 2000`. These fix the memory map that `mld -b 256` assumes.
- **`fence/makefile` does not depend on the eight files `fence.c` includes.**
  `fence.c:22-46` `#include`s `fence1l`..`fence4r` as initialiser text, but
  `makefile:3` is only `fence.m:	fence.o` and there is no `fence.o:` rule naming them.
  Editing an icon rebuilds nothing. `makefile:1-2` also declare `CC=3cc` and `CFLAGS=-g`
  that `:4`'s hardcoded `3cc fence.o -o fence.m` ignores, so `-g` never reaches the link.
- **`fence.c`'s icon arrays are twelve times larger than the data.**
  `:19` and `:34` declare `short licon[NICONS][HEIGHT*WIDTH]` -- `[4][2304]` -- while each
  included file supplies exactly 48 lines x 4 values = **192 shorts** (verified: all eight
  are 1392 bytes, 48 lines, 192 values). `init()` at `:96` computes the true row width as
  `WIDTH/WORDSIZE+1` = 2 Words = 4 shorts, confirming 192 is right. The two arrays
  therefore occupy 36,864 bytes to hold 3,072 bytes of picture, in a terminal whose whole
  bitmap arena is `90*1024` to `156*1024-1024` (`balloc.c:6-7`). `:106` also sets
  `rbmap[i].rect.corner.y = WIDTH` where every other line uses `HEIGHT`; harmless only
  because both are 48.
- **`cent` is called three different things.** `cent/Makefile:2` says
  `'Makefile' for CENTIPEDE by Pat Autilio	11/3/83`, the target is `cent.m`, and
  `player.c:3` -- the file holding `main()` -- says `'main' for Dougslug contains the
  main timing loop.` No `cent.m` is installed anywhere (`v10/usr/jerq/mbin/` has 58 `.m`
  files and none is `cent.m`), and `main()` is not in a file named for the program.
- **`cent/exit_game.c:3` documents a score file that does not exist.** `" * 'exit_game'
  updates score file if necessary and exits or starts"` -- the function (`:6-31`) does no
  file I/O at all, so there is no score-file destination to honour.
- **Two source files carry defects a build will not catch.** `bounce.c:10-11` is missing a
  comma -- `7, -11` then `-5, 3,` -- so `-11 -5` folds to a single `-16` and every later
  initializer shifts by one: `dfrom = foo[2]` gets `{7,-16}` and `dto = foo[3]` gets
  `{3,13}`, not the written values. (The sibling `moire.c:8-15` has the same table with
  its commas intact.) `m.c:40-47` `lim5()` returns nothing when `-5 <= v <= 5`, the
  common case, and its result is assigned to `vx0`/`vx1` at `:72` and `:81`.
- **Strings that look like install paths but are screen decoration.** `fake.c:17,32`
  `"ikeya!/usr/rob/jerq"` and `"alice!/usr/rob/jerq"` are lines of a fake `sh` session
  drawn into a window; `fake.c:34` `"#include <jerq.h>"` is likewise display text, not a
  real include. A grep for paths in this tree will hit them.
- **`make` in `v10/usr/jerq/demo/src` DELETES and BUILDS NOTHING.** `makefile:4-5` puts
  `clean:` / `rm -f *.o` FIRST, so it is the default target. Every demo is an explicitly
  named target; a bare `make` removes objects and exits 0.
- **`demo/src/makefile:1-3` names a compiler the file never uses.** `BIN=/usr/dud/bin`,
  `CC=$(BIN)/3cc`, `AS=$(BIN)/3as`; `$(CC)` and `$(AS)` appear in no rule -- every rule
  invokes `mcc`/`mld` from `PATH`. `/usr/dud/` occurs nowhere in the repository
  (`git ls-files | grep -c '/dud/'` = 0). Same pattern in `asteroids/makefile:3`
  (`BIN=/usr/jerq/bin`, unused) and `fence/makefile:1-2` (`CC=3cc`, `CFLAGS=-g`, while
  `:4` hardcodes `3cc fence.o -o fence.m` and drops `-g`).
- **`/usr/jerq/bin` DOES NOT EXIST in this distribution** -- `git ls-files
  'v10/usr/jerq/bin*'` returns nothing. `v10/usr/jerq/` has `demo font icon include mbin
  sgs src` and no `bin`. Yet `.profile:6` puts `/usr/jerq/bin` first in PATH,
  `demo/mux/demo:1` repeats it, `demo/mux/demo:14` runs `/usr/jerq/bin/jcif` by absolute
  path, and `cent/Makefile:7` sets `CC=/usr/jerq/bin/3cc`. The real `jcif` is
  `v10/usr/blit/bin/jcif` (plus `v10/usr/blit/mbin/jcif.m`), and the real `jx` is
  `v10/usr/blit/bin/jx` and `v10/usr/630/bin/jx` -- both under a DIFFERENT terminal's tree.
- **`/usr/jerq/lib/l.o` does not exist either.** `git ls-files 'v10/usr/jerq/lib*'` = 0 and
  no `l.o` is tracked anywhere in the repository, so all seven `mld` link lines in
  `demo/src/makefile` reference an absent crt0.
- **`asteroids/makefile` installs to `/usr/jerq/demo/mpx/` while the sibling launcher
  reads `/usr/jerq/demo/mux/`.** `mpx` vs `mux`, one letter apart, and only `mux` exists
  (`git ls-files v10/usr/jerq/demo` -> `mux`, `src`, `stand-alone`; no `mpx`). An
  `asteroids` installed by that rule would be invisible to `demo/mux/demo`, which lists
  its own directory to build the menu.
- **`/usr/jerq/demo/mux/` contains only the launcher.** The one tracked file is
  `demo/mux/demo` itself, and `demo:6` prints the menu with
  `ls /usr/jerq/demo/mux | egrep -v "demo|jciferr" | pr -t -6 -l1` -- which filters out
  the only entry. "Available demos are:" is followed by nothing, and every `32ld $1`,
  `jx $1`, `fflicks /usr/jerq/demo/mux/$1` and `/usr/jerq/bin/jcif ./jcif` branch has no
  file to load. (`jciferr` in the egrep is evidence a file of that name once sat there.)
- **`.blit:2` is missing a path separator.** `PATH=:/usr/blit/bin$PATH` -- no `:` before
  `$PATH`, so an inherited `PATH=/bin:/usr/bin` becomes
  `:/usr/blit/bin/bin:/usr/bin`: `/usr/blit/bin` is glued to the first inherited entry and
  neither `/usr/blit/bin` nor `/bin` is searched. It works only if the inherited PATH
  already begins with `:`. Note `.profile:6` in the same home directory sets
  `TERM=5620` and a path with no `/usr/blit/bin` at all, so the two dotfiles configure two
  different terminals for the same user.
- **Committed build products sit beside their sources.**
  `v10/usr/jerq/demo/src/cent/cent.m` (38468 bytes) is exactly the default target of
  `cent/Makefile:23`, and `v10/usr/jerq/demo/src/asteroids/a` (21497 bytes) is exactly the
  default target of `asteroids/makefile:7`. Both carry the `.m` magic `70 01 03 00`.
  Neither is in any scan chunk (they are binary), so a text-only pass never sees them, and
  `make` in either directory silently compares timestamps against them.
- **`cent/Makefile:47`'s packaging target obeys a manifest that is one file short.**
  `ls cent.README Makefile playdefs.h *.c | cpio -oc > cent.cpio` -- no `cent.README` is
  tracked anywhere in the repository, so `ls` errors on it and the cpio archive silently
  omits the README the author meant to ship.
- **`demo/src/makefile:28-29` builds a target whose sources are absent.** `w: w.o
  newlayer.o` / `mcc -o w -j w.o newlayer.o -llayer`; there is no `w.c` in the directory
  (the only tracked `w.c` is `v8/usr/src/cmd/w/w.c`, an unrelated VAX command) and
  `newlayer.c` lives in `v10/usr/jerq/src/lib/layer/`, not here. `moire:` and `rae:`
  (`:43-48`) declare no prerequisites at all, so they rebuild unconditionally.
- **`v10/usr/jerq/demo/src` and `v10/usr/blit/demo/src` are a diverged duplicate pair.**
  50 names in common: **25 byte-identical, 25 differ** -- including `makefile`,
  `asteroids/makefile`, `asteroids/a.c`, `bomb.c`, `image.c`, `rock.c`, `rock.h`,
  `ship.c`, and `a.c`, `b.c`, `ball.c`, `bltdemo.c`, `bounce.c`, `clock.c`, `disc.c`,
  `m.c`, `maxwell.c`, `moire.c`, `road.c`. Worse, three names are a FILE in one tree and a
  DIRECTORY in the other: jerq has `alphabet` (432 B), `pacman` (384 B) and `smiley`
  (96 B) as binaries, where blit has `alphabet/`, `pacman/` and `smiley/` as source
  directories. Picking a file by name across the two trees is not safe.
- **`v10/usr/include/sys/lnode.h` is not from the tape -- it is an ipnx reconstruction,
  and it says so in its own first twelve lines.**
  `lnode.h:4-11`: `" * RECONSTRUCTED BY ipnx.  This file is not in the v10src tarball, and neither"` /
  `" * is <sys/share.h> or <sys/retlim.h>, although lsys/os/limits.C includes all"` /
  `" * three ... every offset and size is"` / `" * confirmed against Bell Labs' 1989 objects in libc.a.  See"` /
  `" * v10/src/PATCHES.md for the disassembly."`
  Confirmed: `git ls-files v10 v10superset | grep -E '(share|retlim)\.h$'` returns nothing,
  so `<sys/share.h>` and `<sys/retlim.h>` really are absent from both trees.
- **`lnode.h:11` cites a path that does not exist.** `v10/src/` is tracked in git zero times
  and is not on disk (the old overlay directory was removed). The only `PATCHES.md` in the
  repository is `v10/usr/src/build/PATCHES.md`. Anyone following the comment finds nothing.
- **`v10/usr/include/sys` and `v10/usr/sys/sys` are a near-duplicate pair of trees.**
  130 names in common, **128 byte-identical**, 2 differ (`ethernet.h`, `types.h`).
  `include/sys` alone has `lnode.h`, `order.h`; `usr/sys/sys` alone has `camac.h`, `ibv.h`.
  The kernel compiles against `usr/sys/sys`; userland against `usr/include/sys`.
- **`v10/usr/include/sys/order.h` is tracked in git but DELETED from the working tree**
  (`git status` reports ` D`, blob `abcb13ba`). It is therefore absent from this scan's
  file list. Any `#include <sys/order.h>` fails right now.
- **`v10/usr/include/sys/types.h` carries an ipnx patch that `v10/usr/sys/sys/types.h`
  does not**: lines 1-2 `#ifndef _TYPES_ /* ipnx guard -- see PATCHES.md */` /
  `#define _TYPES_`, closed by `#endif /* _TYPES_ */`. The kernel copy has no guard.
- **Two DIFFERENT `ethernet.h` files share ONE include guard, `_ETHERNET_`.**
  `v10/usr/include/sys/ethernet.h:1-2` and `v10/usr/include/sys/inet/ethernet.h:1-2` both
  open `#ifndef _ETHERNET_` / `#define _ETHERNET_`, but the first declares
  `struct ether_in` + `struct ether_out` and the second declares `struct etherpup` (and
  `ETHERPUP_PUPTYPE 0x400`, which the other lacks). A translation unit that includes both
  gets only the structs of whichever came first, with no diagnostic. Both also end with an
  unguarded `#ifndef CHANS_PER_UNIT / #define CHANS_PER_UNIT 8` block placed AFTER the
  `#endif`, so that one define does survive either way -- evidence the collision was known
  and worked around for `CHANS_PER_UNIT` only.
- **`v10/usr/include/sys/kb.h` and `v10/usr/include/sys/okb.h` are byte-identical**
  (same blob `17237878`, different inodes -- so not one hard link) and both define
  `struct kb` and `struct kbkmc`; `xkb.h` defines the same two struct tags a THIRD time
  with different members (`struct kbdesc` rings). Any two of the three in one translation
  unit is a redefinition error. `okb.h` is dead ("o" for old) but installed.
- **`ethernet.h` differs materially between the two trees, not cosmetically.**
  `usr/sys/sys/ethernet.h` defines `ETHERALEN` and `struct etherpup` (one struct,
  src+dst+type); `usr/include/sys/ethernet.h` defines `struct ether_in` / `struct
  ether_out` and adds `CHANS_PER_UNIT 8`. A file including the wrong one gets a
  different wire header layout with no diagnostic.

## files with no build contribution

**5 files.**

- `v10/usr/jerq/.jxerr`, `v10/usr/jerq/.jxout`, `v10/usr/jerq/.news_time` -- **deliberately
  zero-byte**, the only empty files in the chunk. The first two are the stdout/stderr
  catchers a `jx` session redirects into (`jx` is `v10/usr/blit/bin/jx` /
  `v10/usr/630/bin/jx`); `.news_time` is a timestamp marker whose *mtime*, not content, is
  the datum. Their presence is the point; a packaging step that skips empty files removes
  them.
- `v10/usr/jerq/demo/src/asteroids/PLAY_ME` -- 32 lines of player instructions, referenced
  by nothing. Note it calls the game "nastyroids" (`:1`), a fourth name for a program the
  makefile builds as `a` and installs as `asteroids`.
- `v10/usr/jerq/demo/src/.comfile` -- two lines, `lunch.c` and `runch.c`: the list of
  files carrying a `/*% ... %` compile line. Read by no makefile here.

Everything else contributes: the 131 headers are compile inputs, the 4 makefiles and
`demo/mux/demo` are build/launch logic, the 8 `fence*` files are `#include`d source, and
the remaining 71 are compiled.

## binary files

**None.** Every one of the 220 files is text (`file` reports text or empty for all 220;
no entry falls outside `*text*`/`empty`).

The eight ASCII-hex bitmap files `v10/usr/jerq/demo/src/fence/fence{1,2,3,4}{l,r}` were
read in full and **carry no text**: for each,
`tr -d '0-9a-fA-FxX, \t\n' < f | wc -c` returns **0**. Each is 1392 bytes, 48 lines,
192 comma-separated `0x....` values, and all eight are distinct (eight different md5s).
Their content is not reproduced here.

Five binaries sit inside this chunk's directories but are NOT in the chunk list (no scan
chunk contains them), and are named here only because the makefiles I read produce them:
`v10/usr/jerq/demo/src/cent/cent.m` (38468 B), `v10/usr/jerq/demo/src/asteroids/a`
(21497 B), and `v10/usr/jerq/demo/src/{alphabet,pacman,smiley}` (432/384/96 B). The first
two begin with the same 4-byte magic `70 01 03 00` as `v10/usr/jerq/mbin/sam.m`.
