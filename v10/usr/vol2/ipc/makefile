all:
	echo "stale: this pre-mk make(1) file's ipcpaper.rf rule cites ipcpaper, which does not exist here (only ipcpaper.rf itself survives); use mk, not make, in this directory (see ./mkfile)." 1>&2
	exit 1
