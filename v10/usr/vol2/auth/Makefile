all:
	echo "stale: this pre-mk Makefile's PARTS/refs.bib/authmgr.8 do not exist here (only auth.ms does), and its %.x rule uses \$% wrong even as GNU Make; use mk, not make, in this directory (see ./mkfile)." 1>&2
	exit 1
