all:
	echo "stale: this pre-mk Makefile's mycite rule uses the undefined \$LIB (should be \$MLIB), and its paper rule cites paper.m, which does not exist here (the source is prefer.m/oprefer.m); use mk, not make, in this directory (see ./mkfile)." 1>&2
	exit 1
